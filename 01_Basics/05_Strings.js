const name="nik"
const repoCount=10

// console.log(name+ repoCount + "Value"); do not write like these make code redable

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);

const gameName=new String('NikNp');
console.log(gameName);